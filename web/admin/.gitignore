# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# `dist` is deliberately NOT ignored: the server embeds it with rust-embed
# and `crates/server/build.rs` fails the build without it, so a fresh clone
# (CI included) has to carry the bundle. See the root .gitignore.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
