name: cratebase
description: Official Dart client SDK for Cratebase — a fast, self-hosted PocketBase-shaped backend (dynamic collections, auth, realtime, files).
version: 0.1.0
homepage: https://github.com/diwa/cratebase
repository: https://github.com/diwa/cratebase

environment:
  sdk: ^3.4.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
